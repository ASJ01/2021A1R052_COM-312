#include <stdio.h>
#include<string.h>
int main()
{
  char a[100]="Hi welcome to miet";
  int length;

  length = strlen(a);

  printf("Length of the string = %d\n", length);

  return 0;
}
